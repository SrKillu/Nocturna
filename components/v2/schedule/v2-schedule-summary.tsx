import { AlertTriangle, CalendarCheck, Clock3, DoorOpen } from 'lucide-react'; import type { ScheduleV2Summary } from '@/lib/types/schedule-v2';
export function V2ScheduleSummary({summary}:{summary:ScheduleV2Summary}){const metrics=[['Sesiones programadas',String(summary.scheduledSessions),CalendarCheck],['Aulas ocupadas',String(summary.occupiedRooms),DoorOpen],['Conflictos',String(summary.conflicts),AlertTriangle],['Próxima sesión',summary.nextSessionLabel,Clock3]] as const;return <section className="grid grid-cols-2 gap-3 lg:grid-cols-4">{metrics.map(([label,value,Icon])=><article key={label} className="rounded-md border bg-card p-4"><div className="flex justify-between"><p className="text-xs text-muted-foreground">{label}</p><Icon className="h-4 w-4 text-primary"/></div><p className="mt-2 text-xl font-semibold">{value}</p><p className="mt-1 text-xs text-muted-foreground">Dato mock</p></article>)}</section>}
