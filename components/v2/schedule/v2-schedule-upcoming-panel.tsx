import { Clock3 } from 'lucide-react'; import type { ScheduleV2Session } from '@/lib/types/schedule-v2';
export function V2ScheduleUpcomingPanel({sessions}:{sessions:readonly ScheduleV2Session[]}){return <section className="rounded-md border bg-card"><div className="border-b p-4"><h2 className="flex gap-2 font-semibold"><Clock3 className="h-4 w-4"/>Próximas sesiones</h2><p className="text-sm text-muted-foreground">Agenda mock sin notificaciones.</p></div><ul className="divide-y">{sessions.map(s=><li key={s.id} className="p-4"><p className="text-sm font-medium">{s.courseLabel} · {s.sectionLabel}</p><p className="text-xs text-muted-foreground">{s.dayLabel} {s.startTime} · {s.roomLabel}</p></li>)}</ul></section>}
