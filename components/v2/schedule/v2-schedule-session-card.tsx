import { Clock3, MapPin } from 'lucide-react'; import { V2ScheduleConflictBadge } from './v2-schedule-conflict-badge'; import { V2ScheduleStatusBadge } from './v2-schedule-status-badge'; import type { ScheduleV2Session } from '@/lib/types/schedule-v2';
export function V2ScheduleSessionCard({session}:{session:ScheduleV2Session}){return <article className="space-y-2 rounded-md border bg-card p-3"><div className="flex items-start justify-between gap-2"><div><p className="text-sm font-medium">{session.courseLabel}</p><p className="text-xs text-muted-foreground">{session.sectionLabel} · {session.teacherLabel}</p></div><V2ScheduleStatusBadge status={session.status}/></div><div className="flex flex-wrap gap-3 text-xs text-muted-foreground"><span className="flex items-center gap-1"><Clock3 className="h-3.5 w-3.5"/>{session.startTime}–{session.endTime}</span><span className="flex items-center gap-1"><MapPin className="h-3.5 w-3.5"/>{session.roomLabel}</span></div><V2ScheduleConflictBadge conflict={session.conflict}/></article>}
