import { V2ScheduleSessionCard } from './v2-schedule-session-card'; import { groupScheduleByDay, type ScheduleV2Session } from '@/lib/types/schedule-v2';
export function V2ScheduleMobileList({sessions}:{sessions:readonly ScheduleV2Session[]}){const groups=groupScheduleByDay(sessions);return <div className="space-y-5 lg:hidden">{Object.entries(groups).filter(([,items])=>items.length).map(([day,items])=><section key={day}><h2 className="mb-2 text-sm font-semibold">{items[0]?.dayLabel}</h2><div className="space-y-2">{items.map((s)=><V2ScheduleSessionCard key={s.id} session={s}/>)}</div></section>)}</div>}
