import { AlertTriangle } from 'lucide-react'; import { Badge } from '@/components/ui/badge'; import type { ScheduleV2ConflictItem } from '@/lib/types/schedule-v2';
export function V2ScheduleConflictsPanel({conflicts}:{conflicts:readonly ScheduleV2ConflictItem[]}){return <section className="rounded-md border bg-card"><div className="border-b p-4"><h2 className="flex gap-2 font-semibold"><AlertTriangle className="h-4 w-4"/>Conflictos mock</h2><p className="text-sm text-muted-foreground">Sin cambios automáticos.</p></div><ul className="divide-y">{conflicts.map(c=><li key={c.id} className="flex justify-between p-4"><div><p className="text-sm font-medium">{c.title}</p><p className="text-xs text-muted-foreground">{c.detail}</p></div><Badge variant="outline">Revisar</Badge></li>)}</ul></section>}
